class Register:
    """
    사용자 등록을 처리하는 도메인 계층의 추상 클래스.

    이 클래스는 사용자 입력에 기반하여 메모리 상의 User 객체를 생성하는 책임을 가지며,
    사용자 유형에 따라 하위 클래스를 통해 구체화된다 (게스트, 일반 사용자, 관리자 등).

    하위 클래스는 사용자 유형에 따른 등록 정책, 유효성 검사, 초기 상태 설정 등을 포함할 수 있다.

    Attributes:

    Methods:

    """
    pass

# ======================================================= #

class GuestRegister(Register):
    """
    (추후 개발)
    비회원(GuestUser) 등록을 처리하는 Register 하위 클래스.

    인증 정보 없이 제한된 접근 권한을 갖는 게스트 사용자를 생성한다.

    특징:
        - 임시 사용자 생성
        - 영속성(DB 저장) 없이 메모리 기반 객체 반환 가능
    """
    pass

class StandardRegister(Register):
    """
    일반 사용자(StandardUser) 등록을 처리하는 Register 하위 클래스.

    이메일 정보를 받아 검증 및 저장하며,
    ID와 PW를 발급한다.

    특징:
        - 이메일 기반 등록
        - ID, PW 발급
        - 유효성 검사 포함 (중복 확인 등)
        - 생성된 객체는 UserRepository를 통해 저장
    """
    pass

class AdminRegister(Register):
    """
    (추후 개발)
    관리자(AdminUser) 등록을 처리하는 Register 하위 클래스.

    시스템 관리 또는 운영 목적으로 특별 권한을 가진 사용자를 등록하며,
    보안 및 인증 조건이 강화될 수 있다.

    특징:
        - 관리자용 전용 인증 정보 필요
        - 높은 서비스 권한 부여
        - 감사 로그 및 접근 제어 연계 가능
    """
    pass
